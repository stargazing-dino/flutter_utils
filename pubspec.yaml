name: flutter_utils
description: My custom utils used across many projects
version: 0.1.4
author: Rex Magana
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
